package is.hbv601g.gamecatalog.helpers;

    // small enum for the different types of collections we have
    // best way to implement multi purpose functions
    // where the caller chooses which functionality they want
public enum GameCollections {
    FAVORITE,
    WANT_TO_PLAY,
    HAS_PLAYED
}
